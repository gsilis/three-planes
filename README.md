# Three Planes

So far, this is a few attempts to create a grid-based game. The `main` branch, and the `grid-storage` branch are two of the attempts.
This uses a standard Vite + React + TypeScript setup.